# 接入指南 · 把本地 NInfer 接到 opencode 等工具

服务是 **OpenAI 兼容 + Anthropic 兼容** 双协议。

**前提**：先 `bash start-server.sh`，等出现 `listening on http://127.0.0.1:8080`。

| 项目 | 值 |
|---|---|
| Base URL（OpenAI） | `http://127.0.0.1:8080/v1` |
| Base URL（Anthropic） | `http://127.0.0.1:8080` |
| Model ID | `bonsai2-27b`（与 `start-server.sh` 里 `MODELID` 一致） |
| API Key | 任意非空，例如 `local` |
| 上下文 | 32768 |
| 制品 | `Bonsai-2-27B-PQ2_0-CRACK.ninfer`（[ModelScope 成品](https://www.modelscope.cn/models/mengyou6688/Bonsai-2-27B-PQ2_0-CRACK-NInfer)；源 GGUF：[CRACK](https://huggingface.co/nuottroisaoduoc/Bonsai-2-27B-Ternary-CRACK-GGUF)） |

---

## 1. opencode

`~/.config/opencode/config.json`：

```json
{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "ninfer": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "NInfer (local)",
      "options": {
        "baseURL": "http://127.0.0.1:8080/v1",
        "apiKey": "local"
      },
      "models": {
        "bonsai2-27b": { "name": "Bonsai 2 27B CRACK (ternary)" }
      }
    }
  },
  "model": "ninfer/bonsai2-27b"
}
```

---

## 2. Claude Code（Anthropic 协议）

```bash
export ANTHROPIC_BASE_URL=http://127.0.0.1:8080
export ANTHROPIC_AUTH_TOKEN=local
export ANTHROPIC_MODEL=bonsai2-27b
```

---

## 3. Continue / Cline / 其它 OpenAI 兼容客户端

- API Base：`http://127.0.0.1:8080/v1`
- Model：`bonsai2-27b`
- API Key：`local`

思维链在 `reasoning_content`；只渲染 `content` 时会先空白一段时间。
